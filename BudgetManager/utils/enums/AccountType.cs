
namespace BudgetManager.utils.enums {
    public enum AccountType {
        SOURCE_ACCOUNT,
        DESTINATION_ACCOUNT,
        DEFAULT_ACCOUNT,
        CUSTOM_ACCOUNT,
        ALL_ACCOUNTS
    }
}
